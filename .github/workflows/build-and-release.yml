name: Build Simulator (DEPRECATED)

# This workflow is deprecated - the project now uses Makefile.kernel for bootable ISO builds
# See build-iso.yml instead

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  build-simulator:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deprecated Notice
      run: |
        echo "⚠️  This workflow is deprecated."
        echo "The project no longer has a simulator build."
        echo "Use the build-iso.yml workflow to build the bootable ISO instead."
        exit 1
